<section class="border-b border-white/10 bg-ink py-14" aria-labelledby="marcas-analises-heading">
	<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
		<p id="marcas-analises-heading" class="text-center text-[10px] font-bold uppercase tracking-[0.4em] text-on-ink-muted">
			Marcas que aparecem nas análises
		</p>
		<ul class="mt-10 flex list-none flex-wrap items-center justify-center gap-x-14 gap-y-8 text-on-ink-body">
			{['Brastemp', 'Consul', 'Electrolux', 'Atlas', 'Itatiaia'].map((name) => (
				<li class="font-display text-xl font-extrabold tracking-tight transition hover:text-pop sm:text-2xl">{name}</li>
			))}
		</ul>
	</div>
</section>
