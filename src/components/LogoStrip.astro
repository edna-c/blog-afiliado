<section class="border-b border-white/10 bg-ink py-14">
	<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
		<p class="text-center text-[10px] font-bold uppercase tracking-[0.4em] text-white/35">Marcas que aparecem nas análises</p>
		<div
			class="mt-10 flex flex-wrap items-center justify-center gap-x-14 gap-y-8 text-white/90"
			aria-label="Marcas citadas"
		>
			{['Brastemp', 'Consul', 'Electrolux', 'Atlas', 'Itatiaia'].map((name) => (
				<span class="font-display text-xl font-extrabold tracking-tight transition hover:text-pop sm:text-2xl">{name}</span>
			))}
		</div>
	</div>
</section>
